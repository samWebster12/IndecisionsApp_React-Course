{
  "name": "indecisions",
  "version": "1.0.0",
  "main": "index.js",
  "author": "samWebster12",
  "license": "MIT",
  "scripts": {
    "compile:app": "babel src/app.js --out-file=public/scripts/app.js --presets=env,react",
    "watch:app": "babel src/app.js --out-file=public/scripts/app.js --presets=env,react --watch",
    "compile:other": "babel src/playground/counter-example.js --out-file=public/scripts/app.js --presets=env,react",
    "watch:other": "babel src/playground/counter-example.js --out-file=public/scripts/app.js --presets=env,react --watch"
  },

  "dependencies": {
    "babel-preset-env": "1.5.2",
    "babel-preset-react": "6.24.1"
  }
}
